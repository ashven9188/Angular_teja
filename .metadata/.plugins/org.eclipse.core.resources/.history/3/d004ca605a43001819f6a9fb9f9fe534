import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Ass1bComponent } from './ass1b.component';

describe('Ass1bComponent', () => {
  let component: Ass1bComponent;
  let fixture: ComponentFixture<Ass1bComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Ass1bComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Ass1bComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
